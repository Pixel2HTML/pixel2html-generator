{
  "name": "pixel2html-<%= clientId %>-<%= projectName %>",
  "description": "pixel2html <%= clientId %>/<%= projectName %>",
  "license": "MIT",
  "repository": "http://code.pixel2html.com",
  "version": "1.0.3",
  "scripts": {
    "install": "npm install && bower install",
    "start": "npm run install && gulp serve",
    "build": "npm run install && gulp"
  },
  "dependencies": {
    "del": "^2.2.2",
    "gulp": "^3.9.1",
    "browser-sync": "2.6.5",
    <% if (cssProcessor === 'scss' || frontEndFramework) { -%>
    "gulp-sass": "^2.0.4",
    <% } -%>
    <% if (cssProcessor === 'less') { -%>
    "gulp-less": "^3.0.3",
    <% } -%>
    <% if (cssProcessor === 'styl') { -%>
    "gulp-stylus": "^2.0.6",
    <% } -%>

    "gulp-autoprefixer": "^2.3.1",
    "gulp-clean-css": "^2.0.12",
    "gulp-concat": "^2.6.0",
    "gulp-uglify": "^1.5.3",
    "gulp-jshint": "^1.12.0",

    "gulp-sourcemaps": "^1.6.0",

    "gulp-util": "^3.0.7",
    "gulp-plumber": "^1.1.0",
    "gulp-rename": "^1.2.2",

    "require-dir": "^0.3.0",
    "gulp-zip": "^3.2.0"


  }
}
